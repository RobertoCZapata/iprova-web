# iPROVA - Development Rules & Guidelines

> Este archivo contiene las reglas y convenciones que **SIEMPRE** debemos seguir en el proyecto.

## 🎯 Principios Fundamentales

1. **Escalabilidad**: Código fácil de extender sin modificar componentes existentes
2. **Mantenibilidad**: Todo el contenido editable en `lib/data.ts`
3. **Clean Code**: Código legible, bien tipado y documentado
4. **Consistencia**: Mismos patrones y convenciones siempre

---

## 📁 Estructura de Archivos

### Componentes
- `components/sections/` → Secciones de página (Header, Hero, Footer)
- `components/ui/` → Componentes UI reutilizables (Button, Card)
- `components/features/` → Componentes específicos de features

**Regla**: Cada componente en su propio archivo, PascalCase para nombres.

### Datos
- `lib/data.ts` → **ÚNICO lugar para contenido editable**
- `lib/utils.ts` → Funciones utilitarias
- `lib/types.ts` → Tipos TypeScript compartidos

**Regla CRÍTICA**: NUNCA hardcodear texto/arrays en componentes. SIEMPRE usar `lib/data.ts`.

---

## 🎨 Convenciones de Código

### TypeScript
- ✅ SIEMPRE tipar props con interfaces
- ✅ NUNCA usar `any`
- ✅ Exportar tipos cuando se reutilicen

### React/Next.js
- ✅ Componentes de servidor por defecto
- ✅ `"use client"` solo cuando sea necesario (interactividad)
- ✅ Usar `export function ComponentName()`

### Naming
- Componentes: `PascalCase` (HeroSection)
- Funciones: `camelCase` (formatDate)
- Archivos: `PascalCase.tsx` para componentes

### Imports (orden)
1. React/Next.js
2. Librerías externas
3. Componentes internos
4. Utilidades
5. Tipos
6. Datos

---

## 🎨 Tailwind CSS

- ✅ SIEMPRE usar `cn()` para combinar clases
- ✅ SIEMPRE usar colores del sistema: `bg-primary`, `text-gold`, etc.
- ✅ NUNCA hardcodear colores con valores arbitrarios
- ✅ SIEMPRE mobile-first

### Colores del Sistema
- `bg-primary` / `text-primary` → Navy (#0F172A)
- `bg-gold` / `text-gold` → Gold (#C5A059)
- `bg-gold-light` → Gold Light (#E5C565)
- `bg-background-light` / `text-text-gray`

---

## 📝 Gestión de Contenido

### Regla de Oro
**TODO el contenido editable debe estar en `lib/data.ts`**

### Qué va en `lib/data.ts`:
✅ Textos, arrays, objetos de contenido
✅ Servicios, estadísticas, features
✅ Información de contacto
✅ Links de navegación

❌ NO: Lógica de negocio, funciones, tipos complejos

---

## 🖼️ Imágenes e Iconos

### Imágenes
- ✅ SIEMPRE usar `next/image`
- ✅ `priority` solo en hero
- ✅ Alt descriptivo siempre
- ✅ Rutas desde `public/`: `/images/hero.jpg`

### Iconos
- ✅ SIEMPRE usar `lucide-react`
- ✅ Importar específicos (no `*`)
- ✅ Tamaños consistentes: `h-4 w-4`, `h-5 w-5`, `h-6 w-6`

---

## 🚫 Anti-Patrones

❌ NUNCA hardcodear texto en componentes
❌ NUNCA usar `any` en TypeScript
❌ NUNCA componentes sin tipar props
❌ NUNCA valores de color hardcodeados
❌ NUNCA mezclar lógica con presentación

---

## ✅ Checklist para Nuevas Features

- [ ] ¿Contenido en `lib/data.ts`?
- [ ] ¿Componentes tipados?
- [ ] ¿Responsive (mobile-first)?
- [ ] ¿Usa sistema de diseño?
- [ ] ¿HTML semántico?
- [ ] ¿Iconos de `lucide-react`?
- [ ] ¿Imágenes con `next/image`?

---

**Mantener este archivo actualizado** cuando se agreguen nuevas convenciones.

